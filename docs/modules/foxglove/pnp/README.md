# Foxglove PnP 可视化规范

本目录用于维护 PnP 模块在 Foxglove 中的可视化规范与实现任务单。

文档索引：
- `panel-mapping-v1.md`：参数到面板映射（V1）。
- `single-tab-layout-v1.1.md`：单选项卡布局草案（V1.1）。
- `single-tab-panel-spec-v1.2.md`：字段级实现任务单（V1.2）。

统一约束：
- 仅调试模式启用可视化。
- 比赛模式默认关闭可视化，避免影响帧率。
- 几何层默认降频，异常触发短时升频。
